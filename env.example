# Public Clerk identifier. This is safe to expose to the browser.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_replace_me

# Server-side authorization configuration. Never expose the admin user ID in
# client authorization logic and never commit PayPal or Clerk secret keys.
DZONYX_ADMIN_USER_ID=user_replace_me
CLERK_ISSUER=https://replace-me.clerk.accounts.dev
DZONYX_ALLOWED_ORIGINS=https://dzonyx.com,https://www.dzonyx.com
